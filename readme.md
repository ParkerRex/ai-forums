## Convex Table Reset Workaround

If you need to reset/clear all data from a Convex table during development, you can use this workaround:
`npx convex import --replace-all --table mytable --format jsonLines /dev/null -y`

Colors:
#00CADB - main cta one
#1C1B22 - background

the home page:

- shows a preview of the actual thing

my priorities:

- contact page to mimick the one for vercel

home page:

- floating members saying nice stuff
- actual llm you can talk to thats trained on it
- fixed background that looks like poolsuite/designer from cursor
- link to prompts, opens a mini vs code fork

blog

- [ ] marble stuff
      big features:
- treasure / vault
- signal (news aggregator thing)
- multi-index search (see convex article)

what i need to do ...

- does db schema from notion still work for this?
- use v0 to fiddle aorund with making the shell of figma and cursor
