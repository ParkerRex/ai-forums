// Shared member tier type to ensure consistency across components
export type MemberTier = "founding_member" | "early_bird" | "member" | "scholarship";

// You can then import and use this in components:
// import type { MemberTier } from "@/types/member";